# 注意：pydantic 2.x / fastapi 0.115 需要 Python >= 3.8，建议使用 3.10+（本机用 3.12 venv）。
# 说明：已移除未使用的 langchain* 全栈、httpx、reportlab（代码中无引用）。
# Web框架
fastapi==0.115.0
uvicorn[standard]==0.30.0
python-multipart==0.0.9

# Pydantic
pydantic==2.9.0
pydantic-settings==2.5.0

# LLM相关（直接使用 openai SDK；langchain 仅为 tool_registry 的可选软依赖，未安装自动降级）
openai==1.50.0

# 知识图谱
neo4j>=5.25.0,<6.0.0

# 地图数据
requests>=2.32.3,<3
shapely==2.0.6
# CRS / 投影（crs_manager.py 使用 pyproj Transformer）
pyproj==3.6.1

# 工具
python-dotenv==1.0.1
numpy==1.26.4
# requests 兼容性检查要求 chardet < 6.0.0（7.x 会产生 RequestsDependencyWarning）
chardet==5.2.0

# 导出（PNG 导出使用 Pillow；SVG/GeoJSON 为标准库）
Pillow==10.4.0

# 测试
pytest>=8.0
